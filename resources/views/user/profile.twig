{% extends 'templates/user/profile_template.twig' %}

{% block pageTitle %}
    {% if user.username ends with 's' %}
        {{ user.username }}' Lists
    {% else %}
        {{ user.username }}'s Lists
    {% endif %}
{% endblock %}

{% block profile_content %}

    {% include 'user/partials/profile_nav.twig' %}

    <div class="col span_4_of_4">
        {% if not lists.isEmpty() %}
            {% for list in lists %}
                {% include 'templates/list/list_template.twig' %}
            {% endfor %}
        {% else %}
            <h2>This user does not have any lists yet.</h2>
        {% endif %}
    </div>

{% endblock %}